{
  "name": "redux-observable-turbo",
  "private": true,
  "version": "0.0.0-monorepo",
  "type": "module",
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.5.6"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.8.2"
  }
}
